# @then(u'the user should be navigated to the Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user should be navigated to the Review Module page')
#
#
# @then(u'the user can access the notification button on Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user can access the notification button on Review Module page')
#
#
# @then(u'the user can access the logout button on Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user can access the logout button on Review Module page')
#
#
# @then(u'the user can access the field button on Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user can access the field button on Review Module page')
#
#
# @then(u'the user can select All checkbox on Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user can select All checkbox on Review Module page')
#
#
# @then(u'the user use search feature to search between the modules on Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user use search feature to search between the modules on Review Module page')
#
#
# @then(u'the user can sort the table on Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user can sort the table on Review Module page')
#
#
# @then(u'the user can create new Review module from on Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user can create new Review module from on Review Module page')
#
#
# @then(u'the user can edit the previous Review module on Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user can edit the previous Review module on Review Module page')
#
#
# @then(u'the user can delete the unused Review module on Review Module page')
# def step_impl(context):
#     raise NotImplementedError(u'STEP: Then the user can delete the unused Review module on Review Module page')
